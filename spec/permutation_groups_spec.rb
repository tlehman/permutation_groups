require 'spec_helper'

describe PermutationGroups do
  it 'has a version number' do
    expect(PermutationGroups::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
